<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Weather App</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gradient-to-r from-sky-500 to-indigo-500 min-h-screen flex flex-col items-center justify-center p-6"
  >
    <nav class="mb-6 flex justify-center gap-8">
      <a
        href="/"
        class="text-white font-semibold underline text-lg hover:text-yellow-300 transition"
        >⬅️ Back to Home</a
      >
      <a
        href="/weather/all"
        class="text-white font-semibold underline text-lg hover:text-yellow-300 transition"
        >🌞 Show Weathers</a
      >
    </nav>

    <div
      class="bg-white shadow-2xl rounded-2xl p-8 w-full max-w-md hover:scale-[1.02] transition-transform duration-300"
    >
      <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">
        🌤️ Weather Forecast
      </h1>

      <form action="/weather" method="POST" class="space-y-6">
        <div>
          <label for="country" class="block text-gray-700 font-medium mb-1"
            >Country</label
          >
          <input
            id="country"
            name="country"
            type="text"
            autocomplete="off"
            placeholder="Enter country name"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
        <div>
          <label for="city" class="block text-gray-700 font-medium mb-1"
            >City</label
          >
          <input
            id="city"
            name="city"
            type="text"
            autocomplete="off"
            placeholder="Enter city name"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
        <button
          type="submit"
          class="w-full bg-blue-600 text-white font-semibold py-2 rounded-lg hover:bg-blue-700 transition"
        >
          Get Weather
        </button>
      </form>
    </div>
  </body>
</html>
