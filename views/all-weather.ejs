<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>All Cities Weather</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gradient-to-r from-blue-700 via-sky-600 to-indigo-700 min-h-screen p-8"
  >
    <h1
      class="text-4xl text-white font-extrabold text-center mb-10 drop-shadow-lg"
    >
      🌍 Live Weather Dashboard
    </h1>

    <div
      class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 max-w-7xl mx-auto"
    >
      <% weatherData.forEach(city => { %>
      <div
        class="bg-white/20 backdrop-blur-md rounded-2xl p-6 text-white shadow-xl border border-white/30 hover:scale-105 transition-all duration-300 ease-in-out"
      >
        <h2 class="text-2xl font-bold mb-2">
          <%= city.city %>, <%= city.country %>
        </h2>

        <% if (city.icon) { %>
        <img
          src="http://openweathermap.org/img/wn/<%= city.icon %>@2x.png"
          alt="weather icon"
          class="mx-auto w-20 h-20 mb-4"
        />
        <% } %>

        <div class="text-lg space-y-1">
          <p>🌡️ <strong>Temperature:</strong> <%= city.temperature %>°C</p>
          <p>🌤️ <strong>Condition:</strong> <%= city.condition %></p>
          <p>💧 <strong>Humidity:</strong> <%= city.humidity %>%</p>
          <p>🌬️ <strong>Wind Speed:</strong> <%= city.windSpeed %> m/s</p>
          <p>👁️ <strong>Visibility:</strong> <%= city.visibility %> m</p>
          <p>☁️ <strong>Cloudiness:</strong> <%= city.cloudiness %>%</p>
          <p>🔽 <strong>Pressure:</strong> <%= city.pressure %> hPa</p>
        </div>
      </div>
      <% }) %>
    </div>

    <div class="text-center mt-10">
      <a
        href="/"
        class="text-white font-semibold underline text-lg hover:text-yellow-300 transition"
      >
        ⬅️ Back to Home
      </a>
    </div>
  </body>
</html>
